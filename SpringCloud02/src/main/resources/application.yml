server:
  port: 8001
spring:
  application:
    name: SpringCloud02
eureka:
  instance:
    prefer-ip-address: true
    client:
      register-with-eureka: false
      fetch-registry: false
      service-url:
        defaultZone: http://localhost:8761/SpringCloud01